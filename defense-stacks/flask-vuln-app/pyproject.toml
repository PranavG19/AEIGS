[tool.poetry]
name = "flask-vuln-app"
version = "0.1.0"
description = "Intentionally vulnerable Flask app for AEGIS integration testing"
authors = ["AEGIS Test Fixture"]

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.0"
jinja2 = "^3.1"
pyyaml = "5.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
