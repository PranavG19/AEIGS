[project]
name = "aegis-bot-detector"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/detector"]
